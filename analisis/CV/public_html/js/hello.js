newFunction();

function newFunction() {
    var user = prompt('enter the password to enter:');
    while (user == '' || user == null) {
        window.alert('please,enter a paswoord.');
        vas;
        user = prompt('hello da');
    }
    document.write(time + ' ' + user);
    window.alert('welcome' + user + '.');
}
